#!/usr/bin/env bash
CURRENT_DIR="$(dirname $(readlink -f $0))"
RECIPES_DIR="$HOME/layers/custom"
if [ 1 -ne $# ]; then
        echo "bitbake $NAME && bitbake $NAME -c populate_sdk"
        echo 'Need build dir name'
        exit 0
fi
echo "source /home/build/opt/poky/2.5.3/environment-setup-x86_64-pokysdk-linux"
echo "source /home/build/layers/internet/poky/oe-init-build-env $1"
echo "bitbake -c build virtual/kernel && bitbake $NAME && bitbake $NAME -c populate_sdk"
#echo "bitbake $NAME && bitbake $NAME -c populate_sdk"
